mutation CreatePatronSubscription($platform: String!,$planId: String!,$purchaseData:String!, $discountCode: String) {
    createPatronSubscription(platform: $platform, planId: $planId, purchaseData: $purchaseData, discountCode: $discountCode){
        message
        status
    }
}