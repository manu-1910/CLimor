query GetUserProfileByIdQuery($id: Int!){
    getUserById(userId: $id){
        id,
        first_name,
        last_name,
        username,
        description,
        website,
        following_count,
        followers_count,
        images{
            small_url
        }
    }
}