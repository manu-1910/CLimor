mutation SharePodcastMutation(
    $podcastId: Int!,
    $shareCount: Int!
){
    sharePodcast(podcastId : $podcastId, shareCount: $shareCount)
    {
        podcast_id,
        count,
        shared,
        created
    }
}