query Followers($limit: Int!,$offset:Int!) {
    getFollowers(limit: $limit,offset: $offset){
        username,
        description,
        followed,
        images{
            small_url
        }
    }
}