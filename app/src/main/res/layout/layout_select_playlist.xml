<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="44dp"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <ImageView
        android:id="@+id/select_contact_checkbox"
        android:layout_width="20dp"
        android:layout_height="20dp"
        android:padding="0dp"
        android:layout_marginTop="12dp"
        android:src="@drawable/ic_unselected_checkbox"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintHorizontal_bias="0.0"/>

    <TextView
        android:id="@+id/playlist_name_text_view"
        android:layout_width="wrap_content"
        android:layout_height="44dp"
        android:text="Top Trending Now"
        android:textSize="@dimen/textSize14"
        android:gravity="center_vertical"
        android:textColor="@color/black"
        android:fontFamily="@font/roboto_bold"
        app:layout_constraintStart_toEndOf="@id/select_contact_checkbox"
        app:layout_constraintTop_toTopOf="parent"
        android:layout_marginStart="@dimen/marginMediumSmall"/>


</androidx.constraintlayout.widget.ConstraintLayout>